// Sedinta 4 - 22/03/2025// Coada + Stiva#define _CRT_SECURE_NO_WARNINGS#include<stdio.h>#include<stdlib.h>#include<string.h>#include<malloc.h>typedef struct nod nod;typedef struct student student;typedef struct LD LD;struct student {	unsigned id;	char* nume;	int nrMaterii;	float* note;};student creareStudent(unsigned id, const char* nume, int nrMaterii, float* note) {	student s;	s.id = id;	s.nume = malloc(sizeof(char) * (strlen(nume) + 1));	strcpy_s(s.nume, sizeof(char) * (strlen(nume) + 1), nume);	s.nrMaterii = nrMaterii;	s.note = malloc(sizeof(float) * nrMaterii);	for (int i = 0; i < nrMaterii; i++) {		s.note[i] = note[i];	}	return s;}void afiseazaStudent(student stud) {	printf("Student -> Id: %d | Nume: %s | NrMAterii: %d | Note:",		stud.id, stud.nume, stud.nrMaterii);	for (int i = 0; i < stud.nrMaterii; i++) {		printf("\n  Nota %d: %.2f", i + 1, stud.note[i]);	}	printf("\n");}struct nod {	student info;	nod* next;};//void main() {//	int note[3] = { 7, 8, 9 };//	student s1 = creareStudent(1, "Popescu Ionut", 3, note);//	afiseazaStudent(s1);////	student* pointerStud = &s1;//////	//char liniebuffer[100];//	//char* token;//	//char delimitator[] = ",\n";//\n////	//FILE* f = fopen("Text.txt", "r");//	//if (f) {//	//	while (fgets(liniebuffer, sizeof(liniebuffer), f)) {//liniebuffer="111,Popescu Alin,2,10,9"////	//		token = strtok(liniebuffer, delimitator);//token="111"////	//		s.id = atoi(token);//s.id=111////	//		token = strtok(NULL, delimitator);//token="Popescu Alin"//	//		s.nume = malloc(sizeof(char) * (strlen(token) + 1));//	//		strcpy_s(s.nume, (strlen(token) + 1), token);//***//////	//		token = strtok(NULL, delimitator);//token="2"//	//		s.nrMaterii = atoi(token);//s.nrMaterii=2////	//		s.note = malloc(sizeof(float) * s.nrMaterii);//	//		for (int i = 0; i < s.nrMaterii; i++) {//	//			token = strtok(NULL, delimitator);//token="10",token="9"//	//			s.note[i] = atof(token);//s.note[i]=10;s.note[i]=9//	//		}////	//		lista = inserare(lista, s);////	//		free(s.note);//	//		free(s.nume);//	//	}//	//}////	//fclose(f);//	//afisare(lista);//	//dezalocare(lista);////}int main() {    int note[3] = { 7, 8, 9 };    student s1 = creareStudent(1, "Popescu Ionut", 3, note);    afiseazaStudent(s1);    student* pointerStud = &s1;    return 0;}